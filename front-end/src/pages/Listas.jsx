import React from 'react'
import { useParams } from 'react-router'


export default function Listas() {
  const { id } = useParams()
  console.log(id)
  return (
    <section>
     
    </section>
  )
}
